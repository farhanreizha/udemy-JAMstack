import { css } from '@emotion/react'

export const copyrightIcon = css`
  position: relative;
  top: 0.2ch;
`

export const note = css`
  display: block;
  margin: 5ch auto 1ch;
  text-align: center;
  color: var(--color-text-invert);
`

export const trademark = css`
  width: 3em;
  align-self: center;
  color: var(--color-text-invert);
`
